#include <stdio.h>

int main(){
    int x;
    scanf("%d", &x);
    for (int i = 1; i == 9, i++){
        printf("%d * %d = %d", x, i, x * i);
    }
}
/* for(초기화, 조건, 행위)
 i++ : 1만큼 증가(++i : 실행 전 1만큼 증가), i-- : 1만큼 감소*/